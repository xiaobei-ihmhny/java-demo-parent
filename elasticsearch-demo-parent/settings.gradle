rootProject.name = 'elasticsearch-demo-parent'
rootProject.children.each {project ->
    project.buildFileName = "${project.name}.gradle"
}
include 'elasticsearch-javaapi-demo'
include 'elasticsearch-rest-high-level-client-demo'

